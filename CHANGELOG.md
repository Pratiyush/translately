# Changelog

All notable changes to Translately are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html). Pre-1.0 (`0.x`) tags are marked as prereleases on GitHub.

## [Unreleased]

### Added
- **ICU MessageFormat validator** (T203, closes #44). New `IcuValidator` in `:backend:service/translations` parses ICU source via `com.ibm.icu:icu4j:76.1` `MessagePattern` and returns a structured `ValidationResult` with line+col-tagged errors. Rejects malformed syntax, missing `other` branches on plural/selectordinal/select, and unknown SIMPLE argument types; accepts empty/blank sources (`TranslationState` gates export, not the validator). Consumers: editor autosave (T207) and JSON importer (T301). 13 Kotest cases cover the full matrix — English + Russian + Serbian CLDR plurals, nested plural-in-select, multi-line line+col reporting. New arch page at `docs/architecture/icu-validation.md`.
- **Postgres full-text + trigram key search** (T206, closes #47). New Flyway migration `V4__keys_fts_trigram.sql` enables the `pg_trgm` extension, adds a generated `keys.search_vector` column (`tsvector` over `key_name + description`, `GENERATED ALWAYS AS ... STORED`), a GIN index on that vector (`idx_keys_search_vector`), and a trigram GIN index on `translations.value` (`idx_translations_value_trgm`). New `KeySearchService` (`:backend:service/keys`) composes a native-SQL query from a `KeySearchQuery` DTO: filters by namespace slug, tag intersection (AND), lifecycle state; pagination via `limit` / `offset`; membership-gated so non-members see `NOT_FOUND` (no project-existence disclosure). Primary path is FTS (`plainto_tsquery('simple', …)` against the vector) with `ts_rank` surfaced as `matchRank`; falls through to trigram similarity on `translations.value` when FTS has no hits. 8 new integration tests (`KeySearchServiceIT`) exercise the composition against a real Postgres — free-text hits, namespace + tag-intersection + state filters, pagination stability, trigram fallback, membership gating, empty-query browse. Seed helper `KeysTestHelpers` factored out for reuse by T207 / T208. 6 new `MigrationV4Test` assertions cover schema shape, extension presence, generated-column behaviour, and GIN index creation. Text-search configuration is deliberately `'simple'` (no language-specific stemming) — see [ADR 0003](docs/architecture/decisions/0003-postgres-fts-over-elasticsearch.md). New docs: [`docs/architecture/search.md`](docs/architecture/search.md) + ADR 0003; `data-model.md` extended with a V4 section; `llms.txt` + `llms-full.txt` regenerated.
- **Backend CRUD for keys, namespaces, and translation cells** (T208 backend half; pairs with the forthcoming webapp PR to close #48 + #49). New `KeyService` in `:backend:service/keys` covers list / create / get / rename / soft-delete for keys and list / create for namespaces; `upsertTranslation` flips the Translation row's state and writes an `Activity(TRANSLATED)` row. New REST surface at `/api/v1/organizations/{orgSlug}/projects/{projectSlug}/keys` + `/namespaces` with matching DTOs and the uniform `OrgException` error envelope. `TranslationActivity` rows fire on every create / update / state-change / translate / delete. 9 `@QuarkusTest` integration tests (`KeyResourceIT` 5 + `NamespaceResourceIT` 4) cover the happy paths, validation 400s, 404 on unknown / non-member access, translation upsert idempotence, and the namespace slug-collision 409. OpenAPI schema regenerated (5 new paths, 13 new operations). New API-ref page at [`docs/api/keys-and-namespaces.md`](docs/api/keys-and-namespaces.md). Tag CRUD + the `GET /keys/{id}/activity` timeline + webapp wiring ship in follow-up PRs.
- **Phase 2 data model — translation keys, namespaces, tags, translations, per-key comments + activity log** (T201 + T202, closes #42 + #43). Seven new JPA entities under `:backend:data` (`Namespace`, `Tag`, `Key`, `KeyMeta`, `Translation`, `Comment`, `Activity`) + three enums (`KeyState`, `TranslationState`, `ActivityType`). Flyway migration `V3__keys_translations_icu.sql` creates eight tables (`namespaces`, `tags`, `keys`, `key_meta`, `key_tags` join, `translations`, `key_comments`, `key_activity`) with matching `CHECK` / `UNIQUE` / `FK ON DELETE CASCADE` constraints and hot-path indexes. New test: `MigrationV3Test` (Testcontainers Postgres 16) asserts schema shape, cascade deletes, CHECK rejections, and unique-constraint enforcement; `KeysAndTranslationsTest` covers entity defaults + slug normalisation + enum exhaustiveness. [ADR 0002](docs/architecture/decisions/0002-translation-state-machine.md) documents the 5-state translation lifecycle (`EMPTY / DRAFT / TRANSLATED / REVIEW / APPROVED`). `docs/architecture/data-model.md` extended with the V3 section + hot-path index catalogue. Note: ticket title said `V2__keys_translations_icu.sql` but `V2` was already taken by the Phase 1 auth-tokens migration, so the file ships as `V3`.
- **Product-doc screenshots, light + dark** (`docs/product/screenshots/`). 14 PNGs across the public auth surface (`signin`, `signup`, `forgot-password`, `verify-email-pending`) and the authenticated shell (`dashboard`, `orgs`, `projects`), embedded in the matching product pages via `<picture>` with `prefers-color-scheme` so the docs site automatically shows the right variant. New capture workflow lives in `webapp/scripts/capture-screenshots.mjs`; run via `pnpm --filter @translately/webapp screenshots` after `pnpm --filter @translately/webapp dev` is up. `playwright` added as a webapp devDependency. Also adds `docs/product/screenshots.md` explaining the regen steps.
- **API-key + PAT authentication enforcement** ([T110-enforce](https://github.com/Pratiyush/translately/issues/149)). Two new JAX-RS filters in `:backend:api/security`:
  - `ApiKeyAuthenticator` (runs at `Priorities.AUTHENTICATION`) — parses `Authorization: ApiKey <prefix>.<secret>`, looks up `api_keys.prefix`, Argon2id-verifies the secret, checks `revoked_at` / `expires_at`, pushes the row's `scopes` column into `SecurityScopes`, and binds the owning org's slug into `TenantContext`. Unknown prefix and bad secret collapse to one 401 `UNAUTHENTICATED` response; revoked and expired get their own codes (`CREDENTIAL_REVOKED`, `CREDENTIAL_EXPIRED`).
  - `PatAuthenticator` (same priority) — parses `Authorization: Bearer tr_pat_<prefix>.<secret>`, distinguishes from JWT by the `tr_pat_` literal prefix, and intersects the PAT's stored scopes with the owning user's **current** effective scope set (computed via `ScopeResolver` from `OrganizationMember` rows). A PAT minted while the user was ADMIN of an org it's now only MEMBER of loses its ADMIN-only scopes on every subsequent request — no revoke needed.
- **`CredentialAuthenticator`** (`:backend:service/credentials`) — the DB-touching boundary between the API module and `:backend:data`. Single entry-point for both authenticator filters; returns a sealed `CredentialAuthResult` that the filter maps onto HTTP. Stamps `last_used_at` synchronously in the same transaction (a Quartz-batched async update is tracked as a follow-up).
- **`NonJwtBearerAuthMechanism`** (`:backend:api/security`) — HTTP auth mechanism at priority 2000 that intercepts `Authorization: ApiKey ...` and `Authorization: Bearer tr_pat_...` before smallrye-jwt's mechanism (priority 1000) sees them. Returns a placeholder authenticated identity so proactive auth doesn't 401 a non-JWT bearer shape; the real credential verification still runs in the JAX-RS filter where it shares `SecurityScopes` with the JWT path.
- **`JwtSecurityScopesFilter`** now tolerates non-JWT principals — accessing the injected `JsonWebToken` on an API-key / PAT request throws `IllegalStateException`, which the filter catches and treats as "no JWT scopes to contribute".
- **Integration tests** (`:backend:app/test/.../security`): `ApiKeyAuthenticatorIT` (9 cases) and `PatAuthenticatorIT` (8 cases) mint real credentials via `ApiKeyService` / `PatService` and exercise the full chain (happy path, revoked, expired, bad secret, unknown prefix, malformed token, other-scheme header ignored, and — for PATs — cross-org scope intersection where a MEMBER cannot exercise an ADMIN-scoped PAT).

### Documentation
- `docs/architecture/auth.md` — credential-type matrix refreshed (header format, scope source, TTL), new "Coexistence with the JWT mechanism" section explains `NonJwtBearerAuthMechanism`, and the module layout now shows the full filter / mechanism / service tree.
- `docs/api/auth.md` — Authorization header format table now documents all three credential types exactly as they go on the wire (`ApiKey tr_ak_<8>.<43>`, `Bearer tr_pat_<8>.<43>`, `Bearer <jwt>`), dispatch rules, and the `UNAUTHENTICATED` / `CREDENTIAL_REVOKED` / `CREDENTIAL_EXPIRED` / `INSUFFICIENT_SCOPE` failure-mode table.
- `docs/product/api-keys-and-pats.md` — "Authentication on protected endpoints" section rewritten to note enforcement is live, with worked examples for PAT scope intersection at request time.

## [0.1.0] — 2026-04-18 — Phase 1: Auth + Organizations

### Added (Phase 1)
- **JPA entities for auth + orgs** (`:backend:data`): `User`, `Organization`, `OrganizationMember`, `Project`, `ProjectLanguage`, `ApiKey`, `Pat`. Common `BaseEntity` superclass with auto-maintained `createdAt` / `updatedAt` + external ULID. Enums: `OrganizationRole`, `LanguageDirection`, `AiProvider`.
- **ULID generator** (`io.translately.data.Ulid`) — Crockford-base32, 26 chars, monotonic within a single millisecond batch.
- 47 Kotest tests covering ULID properties, email normalization, slug normalization, verification status, AI-flag logic, token active/expired/revoked semantics, and entity defaults.

- **Flyway V1 migration** (`backend/data/src/main/resources/db/migration/V1__auth_and_orgs.sql`) — 7 tables (`users`, `organizations`, `organization_members`, `projects`, `project_languages`, `api_keys`, `personal_access_tokens`), matching FK/unique/check constraints, indexes on hot lookups, `ON DELETE CASCADE` on every child FK. `CHECK` constraints enforce `OrganizationRole`, `LanguageDirection`, `AiProvider` enums and non-negative AI budget.
- Integration test (`MigrationV1Test`, Kotest `DescribeSpec` + Testcontainers Postgres 16-alpine) applies the migration and asserts table shape, unique constraints, cascade deletes, and all three `CHECK` constraint violations surface as SQL errors.
- **Permission scope system** (`:backend:security`): `Scope` enum with 31 tokens covering the v1.0 roadmap (org, members, api-keys, audit, projects, keys, translations, imports/exports, AI, glossaries, screenshots, webhooks, CDN, tasks, branches); `@RequiresScope` annotation (method + class level, method wins); `SecurityScopes` request-scoped bean holding the principal's granted scope set.
- **Scope authorization filter** (`:backend:api`): `ScopeAuthorizationFilter` (JAX-RS `@Provider` at `Priorities.AUTHORIZATION`) enforces the declarative annotation; `InsufficientScopeExceptionMapper` emits a uniform 403 body `{ error: { code: "INSUFFICIENT_SCOPE", details: { required, missing } } }`.
- 40 Kotest assertions: 12 for the enum (naming convention, uniqueness, round-trip serialize/parse, exhaustiveness, declaration-order stability), 18 for `SecurityScopes` (grant/revoke/hasAll/hasAny/missing semantics), plus 10 end-to-end `@QuarkusTest` scenarios for the filter (header-driven allow/deny, method-over-class override, multi-scope requirements, unknown-token tolerance).
- **CryptoService envelope encryption** (`:backend:security`, `io.translately.security.crypto`): AES-256-GCM over a two-layer envelope — a fresh per-secret Data Encryption Key (DEK) encrypted with a single operator-provided Key Encryption Key (KEK). Self-describing format with a leading version byte, IVs, and GCM tags; minimum envelope size 89 bytes. Rotating the KEK means re-encrypting rows (DEKs never need rotation).
- `CryptoServiceProducer` CDI bean reads `translately.crypto.master-key` (env var `TRANSLATELY_CRYPTO_MASTER_KEY`, base64, 32 bytes). Dev/test profiles ship an obvious non-secret placeholder; `%prod` requires the env var to be set at boot (app refuses to start otherwise).
- 26 Kotest assertions covering constructor validation (4), byte-array round-trip including 1 MiB payload (4), UTF-8 string round-trip with emoji (3), non-determinism across 100 encryptions (2), envelope structure + overhead (3), AEAD tamper resistance for every mutable byte region and truncation and wrong-KEK (6), version-byte handling + too-short inputs (3), and a plaintext-leak guard.
- **PasswordHasher** (`:backend:security`, `io.translately.security.password`) — Argon2id wrapper with OWASP-recommended defaults (m=64 MiB, t=3, p=4). Hashes user passwords, API-key / PAT secrets, and email-verification / password-reset token bodies. Uses the self-describing Argon2 output so parameter upgrades don't need schema changes. Verification is constant-time and never throws on malformed input (returns `false` instead).
- **TokenGenerator** (same package) — SecureRandom → base64url-without-padding single-use tokens. 32 raw bytes → 43 URL-safe characters, with an optional human-readable prefix for API keys and PATs (e.g. `tr_apikey_…`).
- 24 Kotest assertions: 14 for `PasswordHasher` (output shape, parameter embedding, salt freshness, verify for correct / wrong / case-mismatched / null / blank / malformed inputs, empty password, 1 KiB password, Unicode+emoji, multi-user cross-contamination guard); 10 for `TokenGenerator` (default length, URL-safe alphabet, 10 000 uniqueness, custom entropy sizes, min/max validation, prefixed output).
- **Tenant request context** (`:backend:security`, `io.translately.security.tenant.TenantContext`) — `@RequestScoped` bean holding the per-request URL-path organization identifier (ULID or slug). Null when the request isn't tenant-scoped (login, signup, `/q/health`, root).
- **Tenant request filter** (`:backend:api`, `io.translately.api.tenant.TenantRequestFilter`) — JAX-RS `@Provider` running at `Priorities.AUTHENTICATION - 100`. Extracts the identifier from `/api/v1/organizations/{orgIdOrSlug}/...` paths and binds it to the request context. Syntactic validation only (ULID = 26-char Crockford base32 or slug = lowercase kebab-case ≤64 chars). Resolution to the internal `organization_id` is the service layer's job. Hibernate filter activation lands with the first DB-touching feature (T103+).
- 14 Kotest assertions: 6 for `TenantContext` (default state, set/clear/replace/blank semantics, ULID-shaped identifier), 5 regex-level for `extractTenant` (ULID path / slug path / missing leading slash / no organizations segment / invalid characters), 3 end-to-end `@QuarkusTest` scenarios exercising the full filter path (tenant-less path, slug path, ULID path).
- **JWT issuer + authenticator** (`:backend:security` + `:backend:api`, `io.translately.security.jwt`). `JwtIssuer` mints short-lived access tokens (default 15 min, carrying `sub` / `upn` / `scope` / `groups` / `orgs` / `typ=access`) and longer-lived refresh tokens (default 30 days, minimal claim set with a single-use `jti`). `JwtSecurityScopesFilter` reads the validated `JsonWebToken` at `Priorities.AUTHENTICATION`, rejects refresh tokens presented as bearer credentials, and unions the access token's scopes into `SecurityScopes`.
- Dev/test RSA keypair committed at `backend/app/src/main/resources/jwt-dev/` with a `README.md` flagging it as non-secret. `%prod` profile overrides via `TRANSLATELY_JWT_PUBLIC_KEY_PATH` / `TRANSLATELY_JWT_PRIVATE_KEY_PATH` env vars.
- Quarkus OIDC extension (on the classpath for Phase 7 SSO/SAML) disabled in the default profile so `smallrye-jwt` cleanly owns `JsonWebToken` production.
- Proactive Quarkus auth (`quarkus.http.auth.proactive=true`) so the `JsonWebToken` is populated for every request, not only endpoints with MicroProfile security annotations — our authorization decisions live in `@RequiresScope`, not `@RolesAllowed`.
- 14 Kotest + JUnit assertions: 10 for `JwtIssuer` end-to-end (claim shape, iss/aud, exp, orgs array, scope order, refresh-token minimal shape + unique jti, RS256 header, expiry-instant round-trip), 4 for `JwtSecurityScopesFilter` via `@QuarkusTest` (unprotected endpoint open, missing token → 401 on `@Authenticated`, wrong scope → 403, refresh token → 403). The full access-token-opens-protected-endpoint happy path lands in T103 with real signup/login wiring.
- **Org-role → scope resolver** (`:backend:security`, `io.translately.security.rbac`). `OrgRoleScopes` object maps each `OrgRole` (OWNER/ADMIN/MEMBER) to a static `Set<Scope>`: OWNER = every scope; ADMIN = every scope minus `project-settings.write`, `ai-config.write`, `api-keys.write`; MEMBER = every `.read` scope plus `keys.write`, `translations.write`, `imports.write`, `ai.suggest`. `ScopeResolver` is an `@ApplicationScoped` pure-function service with `resolveFromMemberships(memberships)` and `canResolveFor(userId, memberships, orgId)` that produce the union of role grants (filtered by orgId when provided). `Membership(organizationId, role)` is a tiny in-module data class — the module stays a pure leaf with no dependency on `:backend:data`; the service layer translates `OrganizationMember` rows into `Membership` before calling.
- 36 Kotest assertions: 21 for `OrgRoleScopes` (exact set content per role, admin exclusions, member writes, strict `OWNER ⊃ ADMIN ⊃ MEMBER` hierarchy with size checks, exhaustiveness — every `Scope.entries` reachable from at least one role), 15 for `ScopeResolver` (empty input → empty set, lone OWNER/ADMIN/MEMBER paths, union across multi-org memberships, duplicate collapse, `orgId` filtering including the unknown-org case, `userId` is non-discriminating) including two Kotest `checkAll` property tests that generate arbitrary membership lists and assert `resolveFromMemberships` equals the explicit union over `OrgRoleScopes.forRole`.
- **Webapp app shell** (`webapp/src/components/shell/`) — persistent nav chrome every authenticated route renders into. `AppShell` lays out `banner` + `main` landmarks; `TopBar` hosts the brand, `OrgSwitcher`, `NavLinks`, `ThemeToggle`, `UserMenu`. `OrgSwitcher` is a Radix dropdown (arrow-key + Esc + focus-return) that alpha-sorts org memberships and stores the active selection in-memory via `AuthStore.setActiveOrg`. `UserMenu` is an avatar-triggered dropdown with profile / settings / sign-out items; sign-out clears the auth store and bounces to `/signin`.
- **Client auth state** (`webapp/src/lib/auth/`) — `AuthStore` is a dependency-free external store (`useSyncExternalStore` compatible) that persists to `localStorage`, re-reads on cross-tab `storage` events, and validates JSON on read (malformed payload → `null`). `useAuth()` React hook exposes `{ user, activeOrg, setActiveOrg, signOut }`. `mockAuth.seedDevUser()` is DEV-only — the sign-in placeholder surfaces a button that calls it so reviewers can click into the shell without a backend.
- **Router** (`webapp/src/router.tsx`, `webapp/src/components/routes/`) — `BrowserRouter` + placeholder routes for `/`, `/orgs`, `/projects`, `/signin`, plus a catch-all 404. `RequireAuth` redirects unauthenticated requests to `/signin` while preserving the attempted location in `state.from` so T117's real sign-in flow can restore it. TanStack Query client hoisted into `App` with `retry: false` + `staleTime: 30_000`.
- **shadcn primitives** (`webapp/src/components/ui/`) — owned-in-tree `Avatar` (wrapping Radix Avatar with fallback semantics) and `DropdownMenu` (full Radix API: trigger, content, item, label, separator, submenu, checkbox item, radio item). All theme colors flow through `hsl(var(--...))` tokens so light / dark switch without rebuilds.
- 34 webapp test assertions added across 5 new suites (`AuthStore.test.ts` ×13, `AppShell.test.tsx` ×3, `TopBar.test.tsx` ×6, `OrgSwitcher.test.tsx` ×10, `UserMenu.test.tsx` ×7) plus `App.test.tsx` expanded to 9 routing + accessibility assertions (authenticated + unauthenticated paths, 404 fallback, axe-clean in light and dark). Total: 89 tests pass, 11 files.

- **Email + password signup flow with verification** (T103). Six new endpoints under `/api/v1/auth/*`: `signup`, `verify-email`, `login`, `refresh`, `forgot-password`, `reset-password`. Documented end-to-end in [`docs/product/auth.md`](docs/product/auth.md).
- **Flyway V2 migration** (`backend/data/src/main/resources/db/migration/V2__auth_tokens.sql`) — three new tables `refresh_tokens`, `email_verification_tokens`, `password_reset_tokens`, each with `external_id` ULID, per-user FK with `ON DELETE CASCADE`, unique token / jti columns, `consumed_at` + `expires_at` for single-use semantics, and `idx_*_user` indexes for lookups.
- **JPA entities** (`:backend:data`, `io.translately.data.entity`): `RefreshToken`, `EmailVerificationToken`, `PasswordResetToken` — each extending `BaseEntity` with an `isUsable` helper that checks expiry + consumption.
- **AuthService** (`:backend:service`, `io.translately.service.auth`) — use-case entry point for the flow. Signup writes an Argon2id password hash, generates a 32-byte random token via `TokenGenerator`, persists its Argon2id hash in `email_verification_tokens`, and sends the verification email. Verify-email consumes the token atomically with flipping `users.email_verified_at`. Login verifies the password hash, refuses unverified users with `EMAIL_NOT_VERIFIED`, and returns a `JwtTokens` pair while recording the new refresh jti in the ledger. Refresh validates the signed JWT via `RefreshTokenParser` (Smallrye `JWTParser`), looks up the `jti`, consumes it atomically, mints a fresh pair, and rejects replays with `REFRESH_TOKEN_REUSED`. Forgot-password is intentionally silent for unknown emails. Reset-password consumes the token and overwrites the password hash in the same transaction.
- **AuthException sealed hierarchy** and **AuthValidator** — boundary validation for every endpoint; stable machine-readable codes (`EMAIL_TAKEN`, `VALIDATION_FAILED`, `EMAIL_NOT_VERIFIED`, `INVALID_CREDENTIALS`, `TOKEN_INVALID`, `TOKEN_CONSUMED`, `TOKEN_EXPIRED`, `REFRESH_TOKEN_REUSED`) mapped at the resource layer to the uniform error envelope specified in `api-conventions.md`.
- **EmailSender** (`:backend:email`, `io.translately.email`) — Quarkus Mailer + Qute templates (`templates/email/verify.html`, `templates/email/reset.html`) with a plain-text alternative for clients without HTML rendering. URL-encodes raw tokens before inlining them; configurable via `translately.mail.from` + `translately.mail.base-url`.
- **RefreshTokenParser** (`:backend:security`) — new CDI-scoped helper that cryptographically validates a refresh JWT (signature + issuer + audience + expiry via Smallrye `JWTParser`) and projects it down to `subject` + `jti`. Used only by the `/auth/refresh` path; bearer-credential access is still blocked by `JwtSecurityScopesFilter`.
- **AuthResource** (`:backend:api`, `io.translately.api.auth`) — JAX-RS resources annotated with `@PermitAll` because these endpoints sit outside the tenant scope (`TenantRequestFilter` leaves the context unbound for non-`/organizations/...` paths). Every endpoint has an `@Operation` + `@APIResponses` annotation so OpenAPI generation is fully populated.
- `JwtTokens` now exposes the freshly-minted `refreshJti` alongside the signed tokens, so `AuthService` can write the ledger row without re-parsing the JWT.
- 42 new assertions: 17 unit tests for `AuthValidator` (email + password + name + token rules, error-code mapping); 5 unit tests for `EmailSender` (URL encoding, trailing-slash normalization, HTML + text rendering for both verify and reset flows); 9 unit tests for the three token entities (`isUsable` truth table, default ULID wiring); and 11 integration tests across `AuthServiceIT` + `AuthResourceIT` + `MigrationV2Test` (happy-path signup → verify → login, duplicate-email rejection, invalid-email / short-password validation, unknown-user login, wrong-password rejection, distinct refresh jtis across logins, refresh rotation, replay detection, password-reset round-trip, every error-body `code` assertion via RestAssured, and raw schema-shape checks for V2). Integration tests run in CI against real Postgres 16 + Mailpit via Testcontainers.

- **API key + PAT issuance / listing / revocation** (T110). Three new modules worth of code:
- **`:backend:service/credentials`** — `ApiKeyService` (project-scoped) and `PatService` (user-scoped) implementing mint / list / revoke with scope intersection at the boundary. `CredentialValidator` runs the structural checks (name 1..128, non-empty scopes, future-dated expiry); unknown scope tokens short-circuit with `CredentialException.UnknownScope`. Tokens are `tr_ak_<8>` (API keys) or `tr_pat_<8>` (PATs) followed by `.<43-char base64url secret>`; the prefix column stores the public identifier, the secret is Argon2id-hashed via `PasswordHasher`. Revoke is idempotent (`revoked_at` stamp).
- **`:backend:api/credentials`** — `ApiKeyResource` at `/api/v1/projects/{projectId}/api-keys` gated by `@RequiresScope(api-keys.write|api-keys.read)`, and `PatResource` at `/api/v1/users/me/pats` gated by `@Authenticated` (any user manages their own). `CredentialErrorMapper` renders the uniform 4xx envelope (`VALIDATION_FAILED`, `UNKNOWN_SCOPE`, `SCOPE_ESCALATION`, `NOT_FOUND`).
- **Scope-intersection enforcement**: mint requests rejected with 403 `SCOPE_ESCALATION` when the requested scope set isn't a subset of the caller's — a non-admin can't fabricate an admin-scoped credential. Details echo requested / held / missing sets for the UI.
- **Tests**: 17 assertions. 10 in `CredentialValidatorTest` (name + scopes + expiry rules, multi-field collection, unknown-token path); 7 in `ApiKeyResourceIT` (mint → list → revoke round-trip, scope escalation, unknown scope, empty name, unknown project, unauthenticated, insufficient scope); 5 in `PatResourceIT` (round-trip, escalation, unknown scope, unauthenticated, cross-user revoke 404). Integration tests boot Postgres 16 via Testcontainers and mint real JWTs via `JwtIssuer`.

- **OpenAPI schema generation + committed `docs/api/openapi.json`** (T113). Quarkus SmallRye OpenAPI now writes the schema to `backend/app/build/openapi/openapi.{json,yaml}` on every `quarkusBuild` (config in `application.yml` under `quarkus.smallrye-openapi.store-schema-directory`). Two new Gradle tasks in `:backend:app`: `copyOpenApi` mirrors the generated JSON into the committed `docs/api/openapi.json`, and `checkOpenApiUpToDate` — wired into `check` so it runs in CI — fails the build with an actionable message when the committed schema drifts from the current controllers. First committed schema covers all 11 endpoints from `/`, `/q/health`, `/api/v1/auth/*` (T103), and `/api/v1/projects/{projectId}/api-keys/*` + `/api/v1/users/me/pats/*` (T110). SDK codegen (T120) consumes this file directly.

- **Auto-generated webapp API client** (T120, `webapp/src/lib/api/`). Typed TypeScript client built on [`openapi-typescript`](https://github.com/openapi-ts/openapi-typescript) (dev dep) + [`openapi-fetch`](https://github.com/openapi-ts/openapi-typescript/tree/main/packages/openapi-fetch) (runtime, ~1 KB gzipped) bound to the committed `docs/api/openapi.json`. `pnpm --filter webapp codegen` regenerates `src/lib/api/types.gen.ts` (750 lines of typed `paths` / components); `pnpm codegen:check` — wired into the webapp CI lint step — fails the PR if the committed types drift from the committed schema, mirroring the backend's `checkOpenApiUpToDate`. The hand-written `createApiClient` wraps openapi-fetch with an optional bearer-token provider (middleware attaches `Authorization: Bearer …` per request) and exposes an `unwrap()` helper that throws `ApiRequestError` on the uniform error envelope so callers can branch on `error.code` rather than HTTP status. Four Vitest assertions cover the token middleware (on and off), 2xx data extraction, and 4xx / 5xx envelope throwing.

- **Webapp auth pages** (T117, `webapp/src/components/routes/auth/`). Five new public routes wire the shell to the real backend: `/signin`, `/signup`, `/verify-email`, `/forgot-password`, `/reset-password`. Each is a React Hook Form + Zod form posting against the typed `api` client (T120); server error codes round-trip via `error.code` and surface as localised strings from `en.json` under the `auth.error.*` namespace, with fall-through to `error.message` for unknown codes. Successful login derives the user shape from the access-token's `sub` / `upn` / `orgs` claims (no `/users/me` round-trip), persists the token pair via `authStore.setTokens()`, and bounces to the pre-auth destination. Verify-email + reset-password pages consume a `?token=…` query param; forgot-password always shows "check your inbox" (mirrors the backend's anti-enumeration 202). The `/signin` route keeps its DEV-only "Seed dev user" button so reviewers can drop into the shell without a running backend. Three Vitest assertions on `SignInRoute` (happy-path login + token storage + navigation, `INVALID_CREDENTIALS` error surfacing, client-side validation blocking empty submit). Adds `react-hook-form` (approved stack entry), `@hookform/resolvers`, and `zod` as dependencies. New `Input` + `Label` shadcn primitives. `i18n/index.ts` gained `{name}` interpolation.

- **Backend organization / project / member CRUD** (T118 + T119 prerequisite). 10 new endpoints under `/api/v1/organizations`: list / create / get-one / rename for orgs; list / role-change / remove for members (invite-by-email deferred to Phase 7); list / create / get-one / update for projects inside an org. `@Authenticated` gates every endpoint; membership is resolved in the service (`OrgService.requireMembership`) so non-members get `NOT_FOUND` rather than disclosure. Slug canonicalisation (`SlugNormalizer`) validates against the same regex `TenantRequestFilter` uses. New `OrgException` sealed hierarchy with stable codes: `NOT_FOUND`, `VALIDATION_FAILED`, `ORG_SLUG_TAKEN`, `PROJECT_SLUG_TAKEN`, `LAST_OWNER` (blocks removing the final OWNER), `INSUFFICIENT_SCOPE`. `docs/api/organizations-and-projects.md` covers the endpoint shape + error table; `docs/api/openapi.json` regenerated from 11 paths (562 lines) to 17 paths (1040 lines). 12 new `@QuarkusTest` assertions across `OrgResourceIT` + `ProjectResourceIT`: round-trips, slug collisions, unknown-org NOT_FOUND, non-member cannot read, unauthenticated 401. Scopes OpenAPI scanning to `io.translately.api` via `mp.openapi.scan.packages` so test-only probe resources can never leak into the committed schema.

- **Webapp organizations, projects, and member management UI** (T118 + T119, `webapp/src/components/routes/`). Real CRUD surfaces wired to the backend via TanStack Query + the typed `api` client: `/orgs` lists every organization the caller belongs to with a Create dialog (name + optional slug), empty-state copy, and role pills; `/orgs/:orgSlug` is a tabbed detail view with Projects (list + Create dialog — name, slug, description, base BCP-47 language tag), Members (inline OWNER/ADMIN/MEMBER select + confirm-to-remove dialog, with a `LAST_OWNER` guard surfaced as a localised error), and Settings (rename). `/projects` follows the active org selected in the header `OrgSwitcher` and links back to `/orgs/:slug` for creation. Reusable Radix `Dialog` primitive in `components/ui/dialog.tsx` with focus trap, Esc-to-close, click-outside, and accessible close. New `@/lib/api/orgs.ts` exposes hook pairs (`useOrgs` / `useCreateOrg` / `useRenameOrg`, `useOrgMembers` / `useChangeMemberRole` / `useRemoveMember`, `useOrgProjects` / `useCreateProject`) with cache invalidation on every mutation. Error envelope → UI copy mapping lives in `@/lib/api/errors.ts`; codes (`ORG_SLUG_TAKEN`, `PROJECT_SLUG_TAKEN`, `LAST_OWNER`, etc.) resolve through `api.error.{CODE}` keys in `en.json` with a sane fallback. Three OrgsRoute Vitest assertions (empty state, populated list links to detail route, create-then-refetch round-trip). Adds `@radix-ui/react-dialog`.

### Infrastructure
- Convention plugin `translately.quarkus-module` now adds `jakarta.persistence.Entity` / `MappedSuperclass` / `Embeddable` to the `kotlin-allopen` annotation set so entity classes are non-final (required by Hibernate proxies).
- `:backend:data` gains `testcontainers-postgresql` + `testcontainers-junit` on the test classpath.

### Documentation
- **Docs site polish** — GitHub Pages now builds with Jekyll + the `just-the-docs` remote theme (search, sidebar navigation across every page, mobile-friendly, auto-rendered markdown). Landing page replaced with `docs/index.md` (Jekyll layout) that surfaces Product / API / Architecture / Self-hosting / Download up-front. Added YAML front-matter to every page so the nav tree is hierarchical, not alphabetical. Cross-tree relative links (`../../backend/...`) rewritten to absolute GitHub blob URLs so they resolve on the live site. `docs/self-hosting/README.md` added as a section index. `.github/workflows/pages.yml` switched from raw upload to `actions/jekyll-build-pages` and now produces a downloadable `docs/downloads/translately-docs.zip` snapshot on every deploy.
- **Docs tree scaffolding** (`docs/product/`, `docs/api/`, `docs/architecture/`) with stub pages for every forward reference so the lychee link-checker stays green; `docs/llms.txt` + `docs/llms-full.txt` land per the [llmstxt.org](https://llmstxt.org) standard, regenerated by `scripts/gen-llms-txt.sh`.
- **Phase 1 architecture back-fill** — `docs/architecture/modules.md` (module graph + ownership), `docs/architecture/data-model.md` (V1 ER diagram + identifier strategy), `docs/architecture/auth.md` (JWT formats, refresh rotation, credential chain), `docs/architecture/authorization.md` (scope naming, role→scope matrix, `@RequiresScope` semantics), `docs/architecture/multi-tenancy.md` (TenantContext + TenantRequestFilter, row-level isolation policy), `docs/architecture/crypto.md` (envelope layout, KEK rotation, operator setup), `docs/architecture/request-lifecycle.md` (filter chain + exception mappings).
- **Phase 1 product back-fill** — `docs/product/app-shell.md` (TopBar / OrgSwitcher / UserMenu / NavLinks walkthrough, keyboard + a11y contract) and `docs/product/theming.md` (three-mode toggle, token catalogue, persistence, reduced-motion / forced-colors behaviour). `docs/architecture/webapp.md` landed alongside with the stack choice rationale, state-model, and routing overview.
- **Phase 1 API back-fill** — `docs/api/scopes.md` (31-token catalogue grouped by phase, wire serialization, role → scope mapping, forward-compat rules), `docs/api/errors.md` (stable `error.code` catalogue including the `INSUFFICIENT_SCOPE` envelope + `WWW-Authenticate` header), `docs/api/rate-limits.md` (per-token sliding-window limits + headers + operator tuning), `docs/api/versioning.md` (path-versioning contract, breaking-vs-additive matrix, deprecation workflow), `docs/api/auth.md` (HTTP-level walkthrough of signup / verify / login / refresh / forgot / reset + credential types + JWT structure + API key minting).
- **V1 migration story** appended to `docs/architecture/data-model.md` with creation order, `ON DELETE CASCADE` coverage, `CHECK` constraints table, unique-constraint catalogue, and the five hot-path indexes V1 creates.
- **ADR 0001** — Argon2id for password and token hashing, capturing parameter choice (m=64 MiB, t=3, p=4) and the rejected alternatives (bcrypt, scrypt, PBKDF2).

## [0.0.1] — 2026-04-17 — Phase 0: Bootstrap

First prerelease. The repository, CI surface, and runtime pipeline exist end-to-end; no product features yet. Next release (v0.1.0, Phase 1) lights up auth and the org / project model.

### Added

**Repository & governance**
- Top-level files: `README.md`, `LICENSE` (MIT), `CHANGELOG.md`, `RELEASE-NOTES.md`, `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, `SECURITY.md`, `CODEOWNERS`.
- Agent-pair-programming context: `CLAUDE.md`, `AGENTS.md`, `.claude/commands/` (`/new-phase`, `/release`, `/check-pr`, `/dogfood-strings`).
- Always-loaded Kiro steering: `.kiro/steering/architecture.md`, `contributing-rules.md`, `api-conventions.md`, `ui-conventions.md`.
- Kiro-style trackers: `tasks.md` (T001–T713) and `_progress.md` (phase dashboard + weekly log).
- Branch protection on `master`: PR required, CODEOWNERS review, signed commits, linear history, no force push / deletions.
- 97 GitHub issues seeded across 8 phase milestones with `type:*` / `scope:*` / `est:*` labels and an MVP partition (`mvp` / `post-mvp` / `deferred`).

**CI / CD**
- `.github/workflows/`: `ci-backend`, `ci-webapp`, `link-checker` (lychee), `codeql` (java-kotlin + javascript-typescript + actions), `release`, `pages`, Dependabot.
- Issue templates (`bug.yml`, `feature.yml`, `question.yml`), PR template with 14-point pre-merge checklist.

**Backend (Quarkus · Kotlin · Java 21)**
- Gradle (Kotlin DSL) multi-module skeleton: `backend/{api,data,service,security,jobs,ai,mt,storage,email,webhooks,cdn,audit,app}` with convention plugins (`translately.base`, `translately.quarkus-module`, `translately.quarkus-app`).
- Version catalog at `gradle/libs.versions.toml` pinning Kotlin 2.1 · Quarkus 3.17 · Kotest / MockK / Testcontainers / ArchUnit.
- Runtime: Quarkus application boots, serves `GET /` (service metadata), `/q/health/{live,ready,started}`, `/q/openapi`, `/q/swagger-ui`. 6 @QuarkusTest assertions.
- CDI bean discovery via `META-INF/beans.xml` in every backend module.
- LDAP extension placeholder values in the default profile so the app boots without LDAP configured (Phase 7 wires real values).

**Webapp (React · Vite · TypeScript · Tailwind · shadcn)**
- pnpm workspace + Vite 6 + React 18 + TS 5.7 strict.
- Design tokens for light + dark in `src/theme/tokens.css`; hsl-var references; honors `prefers-reduced-motion`; persistent focus rings.
- `ThemeProvider` + `ThemeToggle` cycling light → dark → system with `localStorage` persistence and OS-preference reactivity.
- shadcn primitive: `Button` (6 variants × 4 sizes × `asChild`).
- Lucide icons only; `src/i18n/` dogfood wrapper with `en.json`.
- App shell: header/main/footer landmarks, primary nav, metric cards, MVP summary.
- 55 tests across 6 suites (`utils.test.ts`, `i18n.test.ts`, `button.test.tsx`, `ThemeProvider.test.tsx`, `ThemeToggle.test.tsx`, `App.test.tsx`) — all green, no axe violations in light or dark.

**Infra**
- `docker-compose.yml` for dev: Postgres 16, Redis 7, MinIO (auto-created buckets), Mailpit; Keycloak behind `--profile keycloak`.
- `infra/docker/`: `backend.Dockerfile` (JVM fast-jar), `backend.native.Dockerfile` (GraalVM native-image), `webapp.Dockerfile` (nginx static + SPA + `/api` reverse proxy), `nginx.conf`.
- `infra/compose-prod.yml` + `.env.prod.example` for single-host production.
- `docs/` landing page + `self-hosting/hardening.md` placeholder.

### Notes

- No paywalled premium tier: every planned feature (SSO, SAML, LDAP, Tasks, Branching, Webhooks, Glossaries, custom storage, granular permissions, audit) will ship MIT.
- AI is bring-your-own-key, entirely optional. The platform must work end-to-end with zero AI configured.
- Third-party reference sources under `_reference/` are read-only, AGPL-licensed, gitignored, never copied.

[Unreleased]: https://github.com/Pratiyush/translately/compare/v0.0.1...HEAD
[0.0.1]: https://github.com/Pratiyush/translately/releases/tag/v0.0.1
