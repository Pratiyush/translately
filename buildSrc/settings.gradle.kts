// buildSrc settings — shares the root project's version catalog so convention
// plugins can type-safely reference `libs.*` without re-declaring versions.

dependencyResolutionManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
    versionCatalogs {
        create("libs") {
            from(files("../gradle/libs.versions.toml"))
        }
    }
}

rootProject.name = "build-logic"
